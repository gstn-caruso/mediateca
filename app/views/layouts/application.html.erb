<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Mediateca" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Mediateca">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <%# The player controller sits on <body> so that play buttons anywhere on the
      page fall inside its scope. What it drives — the <audio> element and the
      now-playing strip — lives in the permanent #player below, and survives the
      body swap Turbo Drive does on navigation. %>
  <body class="h-full overflow-hidden bg-black text-neutral-200 antialiased" data-controller="player">
    <div class="flex h-full flex-col">
      <div class="flex min-h-0 flex-1 gap-2 p-2">
        <%= render "shared/sidebar" %>

        <main class="min-w-0 flex-1 overflow-y-auto rounded-lg bg-gradient-to-b from-neutral-900 to-neutral-950">
          <%= yield %>
        </main>
      </div>

      <%# relative: the queue positions itself against the player, and opens upward. %>
      <div id="player" data-turbo-permanent class="relative shrink-0 border-t border-white/5 bg-black">
        <%= render "shared/player" %>
      </div>
    </div>
  </body>
</html>
